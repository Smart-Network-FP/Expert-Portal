/*
 * PersonalPage Messages
 *
 * This contains all the text for the PersonalPage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PersonalPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Personal Info Page',
  },
});
