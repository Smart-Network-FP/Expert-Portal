/*
 * DashboardNav Messages
 *
 * This contains all the text for the DashboardNav component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DashboardNav';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardNav component!',
  },
});
