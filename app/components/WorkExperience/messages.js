/*
 * WorkExperience Messages
 *
 * This contains all the text for the WorkExperience component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WorkExperience';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WorkExperience component!',
  },
});
